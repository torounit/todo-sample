import React from 'react'
import { Toolbar, NavItem } from 'rebass';

const Header = () => {
	"use strict";

	return (
		<Toolbar>
			<NavItem children='Todo'></NavItem>
		</Toolbar>
	);
}

export default Header
